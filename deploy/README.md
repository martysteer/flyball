# deploy/ — boot & install

systemd units (`slate-conductor.service`, `spark-controller.service`, `Restart=always`,
`After=network-online.target`) and install scripts. Built at Milestones M4–M5.
See `../docs/03` (Boot straight into the app) for the approach.
