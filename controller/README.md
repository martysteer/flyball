# controller/ — runs on Spark (Unicorn Pi)

The thin client. Renders the 17×7 LED UI from received state and emits button events.
Holds only ephemeral local animation state (scroll position, breathing phase).
See `../docs/02` for the LED layout and button map.

_Empty scaffold — Claude Code fills this in starting at Milestone M0/M1._
