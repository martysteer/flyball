# Runtime dependencies
websockets>=12.0
pydantic>=2.5.0
pillow>=10.2.0
pygame>=2.5.2

# Hardware drivers (Linux/Pi only) — pip install handles Python packages,
# make setup-pi handles system prerequisites (SPI, I2C, lgpio)
unicornhatmini>=0.0.1; sys_platform == 'linux'
inky>=1.5.0; sys_platform == 'linux'
gpiozero>=2.0; sys_platform == 'linux'
