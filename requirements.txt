# Runtime dependencies (app-level)
# Hardware drivers (inky, unicornhatmini, gpiozero, lgpio) installed
# by Pimoroni via 'make setup-pi' — not managed here.
websockets>=12.0
pydantic>=2.5.0
pillow>=10.2.0
pygame>=2.5.2
