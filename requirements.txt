# Runtime dependencies
websockets>=12.0
pydantic>=2.5.0
pillow>=10.2.0
pygame>=2.5.2
# Hardware drivers — pip install handles Python packages,
# make setup-pi handles system prerequisites (SPI, I2C, lgpio)
unicornhatmini>=0.0.1
inky>=1.5.0
gpiozero>=2.0
